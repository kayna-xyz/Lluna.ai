/**
 * @deprecated Prefer `getServiceSupabase` from `@/lib/supabase/admin` for service-role access.
 * Kept for compatibility with older imports.
 */
export { getServiceSupabase as createServiceClient } from '@/lib/supabase/admin'
